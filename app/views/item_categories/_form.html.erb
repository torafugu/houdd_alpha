<%= form_for(@item_category) do |f| %>
  <% if @item_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_category.errors.count, "error") %> prohibited this item_category from being saved:</h2>

      <ul>
      <% @item_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_sym %><br />
    <%= f.select :type_sym, Params::ITEM_SYMBOL_DESCRIPTIONS %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :body_part %><br />
    <%= f.collection_select(:body_part_id, BodyPart.find(:all), :id, :name, :include_blank => true) %>
  </div>
  <div class="field">
    <%= f.label :rq_slot_num %><br />
    <%= f.number_field :rq_slot_num %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
