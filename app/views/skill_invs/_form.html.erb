<%= form_for(@skill_inv) do |f| %>
  <% if @skill_inv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill_inv.errors.count, "error") %> prohibited this skill_inv from being saved:</h2>

      <ul>
      <% @skill_inv.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :skill %><br />
    <%= f.text_field :skill %>
  </div>
  <div class="field">
    <%= f.label :owner_id %><br />
    <%= f.number_field :owner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
