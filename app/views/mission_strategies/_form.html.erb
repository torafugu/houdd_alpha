<%= form_for(@mission_strategy) do |f| %>
  <% if @mission_strategy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mission_strategy.errors.count, "error") %> prohibited this mission_strategy from being saved:</h2>

      <ul>
      <% @mission_strategy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :houdd_user %><br />
    <%= f.text_field :houdd_user %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :script %><br />
    <%= f.text_area :script %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
