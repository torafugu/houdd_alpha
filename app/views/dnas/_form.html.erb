<%= form_for(@dna) do |f| %>
  <% if @dna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dna.errors.count, "error") %> prohibited this dna from being saved:</h2>

      <ul>
      <% @dna.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mob %><br />
    <%= f.text_field :mob %>
  </div>
  <div class="field">
    <%= f.label :gene %><br />
    <%= f.text_field :gene %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
